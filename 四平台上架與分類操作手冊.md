# 四平台上架與分類操作手冊

本文件整理目前專案中四個平台（`Shopee`、`Coupang商城`、`Momo`、`Showmore`）的：
- 使用方法（Python 指令流程）
- bat 使用方法（入口與互動方式）
- 主要參數設定
- 重要資料夾與檔案用途

---

## 0) 快速總覽

- **Shopee**：`shopee/`，重點是先產生大量上架 Excel，再自動上傳。
- **Coupang商城**：`done/coupang商城/`，支援 Google Sheet 與素材圖流程，含 LLM/RAG 分類能力（依 mapping 設定）。
- **Momo**：`done/momo/`，以 `xlsx_to_payload -> momo_auto_pack -> report_goods` 為主流程。
- **Showmore**：`done/showmore/`，從 Google Sheet 組資料後自動上傳。

---

## 1) 系統架構

### 1.1 架構分層

- **入口層（操作入口）**
  - 根目錄 `上架.bat`
  - `done/shared/run_platform_upload.bat`
- **平台流程層（各平台 orchestrator）**
  - Shopee：`shopee/run_shopee_by_row.bat` + Python 腳本
  - Coupang商城：`done/coupang商城/run_coupang_by_row.bat`
  - Momo：`done/momo/run_momo_by_row.bat`
  - Showmore：`done/showmore/run_showmore_by_row.bat`
- **共用能力層（跨平台共用）**
  - `done/shared/llm_core/ollama_client.py`：Ollama client（自動啟動/等待）
  - `done/shared/classify_product.py`：共用 RAG 分類與結果記憶
- **資料與快取層**
  - 各平台 `out/`：輸出/報表
  - 各平台/共用 `cache`、`classify_cache`：embedding 與 result memory
  - `config/*.json`：登入、憑證、mapping 等設定

### 1.2 端到端流程（抽象）

1. 使用者從 `上架.bat` 進入平台選單。  
2. 選平台後，對應 bat 收集必要輸入（row 範圍、帳號、topk 等）。  
3. 平台 Python 腳本讀取來源資料（Google Sheet / xlsx）與設定檔。  
4. 若有分類需求：執行 RAG（檢索 + LLM 驗證），並寫入 result memory。  
5. 產生上架 payload / xlsx / 圖片包。  
6. 呼叫平台上傳流程，並輸出報表到 `out/`。  

### 1.3 分類與記憶庫架構

- **Shopee 專用分類**
  - `shopee/llm_category_service.py`
  - 記憶檔：`shopee/cache/shopee_rag_memory_log.jsonl`、`shopee/cache/shopee_rag_memory_index.json`
- **done 共用分類（Momo/Coupang/Showmore 可共用）**
  - `done/shared/classify_product.py`
  - 記憶檔：`done/shared/data/<channel>/classify_cache/rag_result_memory_log.jsonl`、`.../rag_result_memory_index.json`

---

## 2) 共用入口（bat）

### 1.1 一鍵平台入口

- 主要入口檔案：`上架.bat`（與本文件同層）
- 功能：在 PowerShell 開新視窗後，呼叫 `done/shared/run_platform_upload.bat` 進入平台選單。
- 相容入口：`done/上架.bat`（目前會轉呼叫到根目錄 `上架.bat`）

### 1.2 平台選單 bat

- 檔案：`done/shared/run_platform_upload.bat`
- 功能：選擇平台並呼叫對應 bat：
  - `1` -> `done/coupang商城/run_coupang_by_row.bat`
  - `2` -> `done/showmore/run_showmore_by_row.bat`
  - `3` -> `done/momo/run_momo_by_row.bat`
  - `4` -> `shopee/run_shopee_by_row.bat`
- 內建環境變數（若外部沒設定）：
  - `AUTO_START_OLLAMA=1`
  - `OLLAMA_READY_TIMEOUT=90`

> 註：`AUTO_START_OLLAMA` / `OLLAMA_READY_TIMEOUT` 會被共用 LLM client 讀取，用於需要時自動啟動 Ollama 與等待就緒。

---

## 3) Shopee

### 2.1 主要流程

1. 產生上傳檔：
   - `shopee/build_shopee_mass_upload_with_category.py`
2. 上傳至 Shopee 後台：
   - `shopee/2shopee_bulk_upload.py`

範例（常用）：

```bash
python shopee/build_shopee_mass_upload_with_category.py --use-gsheet --template shopee/MassUploadListingRequestTemplate_TW.xlsm --category-xls shopee/Shopee_category_list.xls --output-xlsx shopee/out/mass_upload_ready.xlsx --use-rag-ai-category --print-category-topk 5
python shopee/2shopee_bulk_upload.py --file shopee/out/mass_upload_ready.xlsx
```

### 2.2 bat 使用方式

- 可直接從平台選單選 `4`（Shopee），會呼叫：
  - `shopee/run_shopee_by_row.bat`
- 也可在 `shopee/` 目錄直接執行 `run_shopee_by_row.bat`。
- bat 會依序執行：
  1. `build_shopee_mass_upload_with_category.py`（含 RAG 分類）
  2. `2shopee_bulk_upload.py`（上傳）

### 2.3 重要參數

`build_shopee_mass_upload_with_category.py`：

- **資料來源**
  - `--use-gsheet`：使用 `done/shared/config/gsheet_pm_source.json`
  - `--input-xlsx`：不用 gsheet 時的來源檔
- **RAG/AI 分類**
  - `--use-rag-ai-category`
  - `--category-top-k`
  - `--min-category-confidence`
  - `--min-category-retrieval-score`
  - `--min-category-retrieval-gap`
  - `--category-force-rebuild-index`
- **Ollama 啟動**
  - `--auto-start-ollama` / `--no-auto-start-ollama`
  - `--ollama-ready-timeout`
- **結果記憶（同商品加速）**
  - `--use-rag-result-memory` / `--no-rag-result-memory`
  - `--reuse-needs-review-memory`
- **輸出與驗證**
  - `--output-xlsx`
  - `--error-xlsx`
  - `--fail-on-category-miss`
  - `--require-dim`
  - `--require-weight`
  - `--print-category-topk N`（注意一定要給數字）

`2shopee_bulk_upload.py`：
- `--file`（必填）
- `--config`
- `--storage`
- `--save-login`
- `--headless`
- `--timeout-ms`

### 2.4 重要資料夾與檔案

- `shopee/Shopee_category_list.xls`：Shopee 類別字典來源。
- `shopee/out/`：輸出上傳檔、錯誤報表。
- `shopee/cache/`：
  - `shopee_categories.json` / `shopee_category_embeddings.npy`：類別索引快取
  - `shopee_rag_memory_log.jsonl`：逐筆分類紀錄（可做訓練素材）
  - `shopee_rag_memory_index.json`：快速命中索引
- `shopee/storage_state.json`：登入狀態快取。

---

## 4) Coupang商城

### 3.1 主要流程

- 建議走 bat：
  - `done/coupang商城/run_coupang_by_row.bat`
- 內部會啟動圖片服務並呼叫：
  - `done/coupang商城/auto_upload.py`
  - `done/coupang商城/create_from_xlsx.py`

### 3.2 bat 使用方式

`run_coupang_by_row.bat` 會互動詢問：
- `START_ROW`
- `ROW_COUNT`

然後自動：
1. 啟動圖片 hosting 視窗
2. 等待 `image_hosting.json`
3. 執行 `auto_upload.py --gsheet-data-start-row ... --gsheet-data-row-count ...`

### 3.3 重要參數

`auto_upload.py`：
- `--image-folder`
- `--gsheet-header-row`
- `--gsheet-data-start-row`
- `--gsheet-data-row-count`

`create_from_xlsx.py`（核心）：
- `--dry-run`
- `--img-base-url`
- `--assets-dir`
- `--image-folder`
- `--delivery-charge`
- `--return-charge`
- `--delivery-charge-type`
- `--free-over`
- `--gsheet-header-row`
- `--gsheet-data-start-row`
- `--gsheet-data-row-count`

> 分類模式通常由 `mapping.json` 的 `category.mode` 控制（例如 `keyword` / `llm`）。

### 3.4 重要資料夾與檔案

- `done/coupang商城/mapping.json`：欄位映射、分類模式、運費等關鍵設定。
- `done/coupang商城/config/login_info.json`：平台登入設定（敏感資料）。
- `done/coupang商城/assets/`：素材圖來源。
- `done/coupang商城/out/`：執行輸出與報表。
- `done/coupang商城/image_hosting.json`：圖片服務資訊（由啟動腳本生成）。

---

## 5) Momo

### 4.1 主要流程

建議走 bat：
- `done/momo/run_momo_by_row.bat`

bat 內部流程：
1. `select_login_info.py --entpCode ...`
2. 逐筆執行 `momotest/xlsx_to_payload.py`
3. `momo_auto_pack.py`（抓圖/驗圖/打包）
4. `report_goods.py`（送出與回報）

### 4.2 bat 使用方式

`run_momo_by_row.bat` 會互動詢問：
- `entpCode`
- `START_ROW`
- `ROW_COUNT`

並逐筆處理（每次 1 筆，利於追蹤）。

### 4.3 重要參數

`momotest/xlsx_to_payload.py`：
- `--data-start-row`
- `--data-row-count`

`momo_auto_pack.py`：
- `--payload`
- `--asset-root`
- `--output-dir`
- `--save-updated-payload`

`select_login_info.py`：
- `--entpCode`

### 4.4 重要資料夾與檔案

- `done/momo/config/all_login_info.json`：多組帳號設定（敏感資料，勿外流）。
- `done/momo/config/login_info.json`：當前選定帳號（由 `select_login_info.py` 產生）。
- `done/momo/momotest/`：payload 與分類流程腳本。
- `done/momo/out/`：執行結果、報告、壓縮檔等。
- `done/shared/assets/`：Momo 素材來源（bat 預設使用）。

---

## 6) Showmore

### 5.1 主要流程

建議走 bat：
- `done/showmore/run_showmore_by_row.bat`

bat 內部流程：
1. 啟動圖片 hosting 視窗
2. 等待 `image_hosting.json`
3. 呼叫 `sheet_to_showmore_upload.py --data-start-row ... --data-row-count ...`

### 5.2 bat 使用方式

`run_showmore_by_row.bat` 會互動詢問：
- `START_ROW`
- `ROW_COUNT`

### 5.3 重要參數

`sheet_to_showmore_upload.py`：
- `--data-start-row`
- `--data-row-count`（`0` 代表不限制，讀到表尾）
- `--header-row`

### 5.4 重要資料夾與檔案

- `done/showmore/config/login_info.json`：登入設定（敏感資料）。
- `done/showmore/out/`：輸出檔與上傳結果。
- `done/showmore/image_hosting.json`：圖片服務連線資訊。

---

## 7) 共用分類與快取（done/shared）

### 6.1 共用 LLM/RAG 位置

- `done/shared/classify_product.py`：共用分類邏輯（含結果記憶）
- `done/shared/llm_core/ollama_client.py`：Ollama client（含自動啟動）

### 6.2 結果記憶與訓練素材

`done/shared/classify_product.py` 會將結果寫到各通路資料夾：
- `done/shared/data/<channel>/classify_cache/rag_result_memory_log.jsonl`
- `done/shared/data/<channel>/classify_cache/rag_result_memory_index.json`

用途：
- `jsonl`：完整歷史，可作人工校正與訓練資料來源
- `index.json`：同商品快速命中，減少重算

### 6.3 Ollama 相關環境變數

- `AUTO_START_OLLAMA=1|0`
- `OLLAMA_READY_TIMEOUT=秒數`

---

## 8) 建議操作順序（實務）

- 日常使用建議先跑根目錄 `上架.bat`，再選平台。
- 需要 Shopee 批次上架時：
  1. 先跑 `build_shopee_mass_upload_with_category.py` 產生 xlsx
  2. 再跑 `2shopee_bulk_upload.py` 上傳
- 要追求分類穩定與速度：
  - 開啟 RAG + result memory（目前預設已開）
  - 定期人工檢視 memory log，回餵規則與資料集。

---

## 9) 安全提醒

- `config/login_info.json`、`all_login_info.json`、憑證 json 都屬敏感資料，避免上傳到公開倉庫。
- 建議將敏感值改為環境變數或本機私有設定檔管理。
